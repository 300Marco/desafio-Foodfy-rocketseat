{% extends "layout.njk" %}

{% block title %}
    <title>Editar - Nome Chef</title>
{% endblock %}

{% block header %}
    {# <header class="header-background">
        <div class="header-menu-box">
            <div class="title">
                <h2 class="sr-only">Admin Foodfy</h2>

                <a href="/admin/recipes">
                    <img src="/assets/admin-logo.png" alt="Foodfy">
                </a>
            </div>
        </div>
    </header> #}
    {% include "chefs/header.njk" %}
{% endblock %}

{% block contents %}
    <div class="recipe-form">
        <form method="POST" action="/admin/chefs?_method=put">
            <div class="recipe-detail">
                <span class="detail-title">Editando Chef</span>

                <div class="buttons">
                    <button class="delete-button" form="form-delete">Deletar</button>
                    
                    <button class="edit-button" type="submit">Salvar Chef</button>
                </div>
            </div>

            {% include "chefs/fields.njk" %}

            <input 
                type="hidden"
                name="id"
                value="{{chef.id}}"
            >
        </form>

        <form id="form-delete" method="POST" action="?_method=delete">
            <input type="hidden" name="id" value="{{chef.id}}">
            {# <button class="delete-button">Deletar</button> #}
        </form>
    </div>
{% endblock %}

{% block footer %} {% endblock %}